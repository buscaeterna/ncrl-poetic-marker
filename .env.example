# Local-only defaults. Choose a private password if binding beyond loopback.
NCRL_POSTGRES_DB=ncrl
NCRL_POSTGRES_USER=ncrl
NCRL_POSTGRES_PASSWORD=ncrl-local-only
# Application has no authentication: do not expose it to the Internet.
NCRL_BIND_ADDRESS=127.0.0.1
